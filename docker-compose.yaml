version: "3.9"

services:
  freellmapi:
    build: .
    container_name: freellmapi
    environment:
      - NODE_ENV=production
      - PORT=3001
      - ENCRYPTION_KEY=a1cc3501b79487748e1fdbfca0e6ced8dbaaf3d186553ea91448bc4023e8a285
      - API_KEY=your_api_key_here
    expose:
      - "3001"
    
    labels:
      - "traefik.enable=true"

      # Route
      - "traefik.http.routers.freellmapi.rule=Host(`freellmapi.bunchoftips.com`)"
      - "traefik.http.routers.freellmapi.entrypoints=websecure"
      - "traefik.http.routers.freellmapi.tls=true"

      # Basic Auth
      - "traefik.http.middlewares.freellmapi-auth.basicauth.users=admin:$$apr1$$tcnw9jz0$$2pkAZlKZcFuYT5UUI7flA."

      # Attach auth middleware
      - "traefik.http.routers.freellmapi.middlewares=freellmapi-auth"

      # Port mapping
      - "traefik.http.services.freellmapi.loadbalancer.server.port=3001"
 
   


    restart: unless-stopped
